{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "cppbuild",
            "command": "g++",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}\\..\\build\\${fileBasenameNoExtension}.exe",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}